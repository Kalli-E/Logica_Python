Atributos -> São o que classificam os objetos (geralmente são as colunas ou os cabeçalhos)
Registros (tuplas) -> São os dados dos atributos (geralmente são as linhas ou as descrições)

Schema -> Banco de dados


